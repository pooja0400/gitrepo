/* Count the no. of occurances of a node (search_for) in a linked list (head)*/

int getCount ( struct Node*head )
{
      //Base case
      if(head==Null)
      return 0;
   
      // Count is 1+ count of remaining list return 1+ getCount (head->next);
}
